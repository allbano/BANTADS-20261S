/**
 * Configuração de ambiente (desenvolvimento).
 * A API Gateway BANTADS é o único ponto de entrada do frontend.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
